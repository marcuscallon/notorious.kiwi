# Build artefacts — regenerated from src/profile.html
/Marcus-Callon-Executive-Profile.pdf
/preview-*.png

# OS / editor
.DS_Store
.vscode/
.idea/

# Node (if we add tooling later)
node_modules/

# Local assets not for committing (e.g. raw headshot masters)
assets/raw/
*.csv
